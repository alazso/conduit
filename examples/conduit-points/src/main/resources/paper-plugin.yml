name: ConduitPointsExample
version: "${version}"
description: Small in-memory Conduit "points" economy provider for Case B testing
authors: [Alazso]
api-version: "26.1"
folia-supported: true
main: so.alaz.conduit.example.points.ConduitPointsPlugin
dependencies:
  server:
    Conduit:
      load: BEFORE
      required: true
